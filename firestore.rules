rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // API server only via Admin SDK (Admin SDK bypasses these rules)
      allow read, write: if false;
    }
  }
}
