// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: incident.query.sql

package dbaccess

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const fetchIncident = `-- name: FetchIncident :one
select i.id,
       i.title,
       i.code,
       i.description,
       i.tenant_id,
       i.created_at,
       sta.status_type,
       sta.name  as status_name,
       sta.color as status_color
from incidents i
         inner join incident_statuses sta on i.latest_status_id = sta.id
where i.id = $1
  and i.tenant_id = $2
`

type FetchIncidentParams struct {
	ID       string `json:"id"`
	TenantID string `json:"tenant_id"`
}

type FetchIncidentRow struct {
	ID          string           `json:"id"`
	Title       string           `json:"title"`
	Code        string           `json:"code"`
	Description *string          `json:"description"`
	TenantID    string           `json:"tenant_id"`
	CreatedAt   pgtype.Timestamp `json:"created_at"`
	StatusType  string           `json:"status_type"`
	StatusName  string           `json:"status_name"`
	StatusColor string           `json:"status_color"`
}

// FetchIncident
//
//	select i.id,
//	       i.title,
//	       i.code,
//	       i.description,
//	       i.tenant_id,
//	       i.created_at,
//	       sta.status_type,
//	       sta.name  as status_name,
//	       sta.color as status_color
//	from incidents i
//	         inner join incident_statuses sta on i.latest_status_id = sta.id
//	where i.id = $1
//	  and i.tenant_id = $2
func (q *Queries) FetchIncident(ctx context.Context, arg FetchIncidentParams) (FetchIncidentRow, error) {
	row := q.db.QueryRow(ctx, fetchIncident, arg.ID, arg.TenantID)
	var i FetchIncidentRow
	err := row.Scan(
		&i.ID,
		&i.Title,
		&i.Code,
		&i.Description,
		&i.TenantID,
		&i.CreatedAt,
		&i.StatusType,
		&i.StatusName,
		&i.StatusColor,
	)
	return i, err
}

const listIncidents = `-- name: ListIncidents :many
select i.id,
       i.title,
       i.code,
       i.tenant_id,
       i.created_at,
       sta.name  as status_name,
       sta.color as status_color,
       sta.status_type
from incidents i
         inner join incident_statuses sta on i.latest_status_id = sta.id
where i.tenant_id = $1
order by i.created_at desc
`

type ListIncidentsRow struct {
	ID          string           `json:"id"`
	Title       string           `json:"title"`
	Code        string           `json:"code"`
	TenantID    string           `json:"tenant_id"`
	CreatedAt   pgtype.Timestamp `json:"created_at"`
	StatusName  string           `json:"status_name"`
	StatusColor string           `json:"status_color"`
	StatusType  string           `json:"status_type"`
}

// ListIncidents
//
//	select i.id,
//	       i.title,
//	       i.code,
//	       i.tenant_id,
//	       i.created_at,
//	       sta.name  as status_name,
//	       sta.color as status_color,
//	       sta.status_type
//	from incidents i
//	         inner join incident_statuses sta on i.latest_status_id = sta.id
//	where i.tenant_id = $1
//	order by i.created_at desc
func (q *Queries) ListIncidents(ctx context.Context, tenantID string) ([]ListIncidentsRow, error) {
	rows, err := q.db.Query(ctx, listIncidents, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListIncidentsRow{}
	for rows.Next() {
		var i ListIncidentsRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.Code,
			&i.TenantID,
			&i.CreatedAt,
			&i.StatusName,
			&i.StatusColor,
			&i.StatusType,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLatestIncidentAssignments = `-- name: ListLatestIncidentAssignments :many
select ra.id,
       ra.incident_id,
       ra.role_id,
       ra.user_id,
       ra.assigned_at,
       ra.assigned_by,
       r.name as role_name,
       r.code as role_code
from incident_role_assignments ra
         inner join incident_roles r on ra.role_id = r.id
where ra.incident_id = $1
  and ra.tenant_id = $2
`

type ListLatestIncidentAssignmentsParams struct {
	IncidentID string `json:"incident_id"`
	TenantID   string `json:"tenant_id"`
}

type ListLatestIncidentAssignmentsRow struct {
	ID         string           `json:"id"`
	IncidentID string           `json:"incident_id"`
	RoleID     string           `json:"role_id"`
	UserID     string           `json:"user_id"`
	AssignedAt pgtype.Timestamp `json:"assigned_at"`
	AssignedBy *string          `json:"assigned_by"`
	RoleName   string           `json:"role_name"`
	RoleCode   string           `json:"role_code"`
}

// ListLatestIncidentAssignments
//
//	select ra.id,
//	       ra.incident_id,
//	       ra.role_id,
//	       ra.user_id,
//	       ra.assigned_at,
//	       ra.assigned_by,
//	       r.name as role_name,
//	       r.code as role_code
//	from incident_role_assignments ra
//	         inner join incident_roles r on ra.role_id = r.id
//	where ra.incident_id = $1
//	  and ra.tenant_id = $2
func (q *Queries) ListLatestIncidentAssignments(ctx context.Context, arg ListLatestIncidentAssignmentsParams) ([]ListLatestIncidentAssignmentsRow, error) {
	rows, err := q.db.Query(ctx, listLatestIncidentAssignments, arg.IncidentID, arg.TenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListLatestIncidentAssignmentsRow{}
	for rows.Next() {
		var i ListLatestIncidentAssignmentsRow
		if err := rows.Scan(
			&i.ID,
			&i.IncidentID,
			&i.RoleID,
			&i.UserID,
			&i.AssignedAt,
			&i.AssignedBy,
			&i.RoleName,
			&i.RoleCode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const storeIncident = `-- name: StoreIncident :one
insert into incidents (
    id, title, code, description, tenant_id, created_at, latest_status_id
) values (
    $1,
    $2,
    $3,
    $4,
    $5,
    current_timestamp,
    $6
) on conflict (id) do update
set title = excluded.title,
    code = excluded.code,
    description = excluded.description,
    tenant_id = excluded.tenant_id,
    latest_status_id = excluded.latest_status_id
returning id, title, code, description, created_at, latest_status_id, tenant_id
`

type StoreIncidentParams struct {
	ID             string  `json:"id"`
	Title          string  `json:"title"`
	Code           string  `json:"code"`
	Description    *string `json:"description"`
	TenantID       string  `json:"tenant_id"`
	LatestStatusID string  `json:"latest_status_id"`
}

// StoreIncident
//
//	insert into incidents (
//	    id, title, code, description, tenant_id, created_at, latest_status_id
//	) values (
//	    $1,
//	    $2,
//	    $3,
//	    $4,
//	    $5,
//	    current_timestamp,
//	    $6
//	) on conflict (id) do update
//	set title = excluded.title,
//	    code = excluded.code,
//	    description = excluded.description,
//	    tenant_id = excluded.tenant_id,
//	    latest_status_id = excluded.latest_status_id
//	returning id, title, code, description, created_at, latest_status_id, tenant_id
func (q *Queries) StoreIncident(ctx context.Context, arg StoreIncidentParams) (Incidents, error) {
	row := q.db.QueryRow(ctx, storeIncident,
		arg.ID,
		arg.Title,
		arg.Code,
		arg.Description,
		arg.TenantID,
		arg.LatestStatusID,
	)
	var i Incidents
	err := row.Scan(
		&i.ID,
		&i.Title,
		&i.Code,
		&i.Description,
		&i.CreatedAt,
		&i.LatestStatusID,
		&i.TenantID,
	)
	return i, err
}
